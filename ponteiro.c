// Variáveis do tipo ponteiro são usadas exclusivamente para armazenar endereços de memória.

int main() {
  int x = 10;
  int *ponteiro; // Declara um ponteiro para um inteiro

  ponteiro = &x; // Atribui o endereço de memória de 'x' ao ponteiro 'ptr'

  
  return 0;
}